# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors and tools
.idea/
.vscode/
*.swp
*.swo

# Per-project environments — machine-specific, never committed anywhere
.envrc
.direnv/
.env.local

# Worktree and agent scratch. workmux puts worktrees outside the repo, but
# per-repo .workmux.yaml overrides may not.
.workmux.local.yaml
.claude/settings.local.json
