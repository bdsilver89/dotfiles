# macOS-only packages. Cross-platform CLI tools live in PKGS_CORE in install.sh,
# where one name drives brew, apt and dnf alike -- adding one here as well would
# mean two edits for one package.
#
# Applied with `brew bundle`. Casks are skipped on a headless install.

brew "tuicr"

cask "alacritty"
cask "ghostty"
cask "visual-studio-code"
cask "zed"
